name: WorkflowTest
  
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        version: [9.0, 8.0]
    
    steps:
      - name: Print GitHub context
        run: |
          echo "${{ toJSON(github) }}"
          echo "${{ github.event_name }}"
          echo "${{ github.events.commits[0].author.name }}"
          echo "${{ github.events.commits[0].message }}"
